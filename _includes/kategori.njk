<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <header>
    <h1><a href="/">Benim Blogum</a></h1>
    <nav>
      <a href="/">Ana Sayfa</a>
    </nav>
  </header>
  <main>
    <section>
      {{ content | safe }}
    </section>
    <section>
      {% set kategoriKey = "category_" ~ kategori_adi %}
      <ul>
        {% for post in collections[kategoriKey] %}
          <li>
            <a href="{{ post.url }}">{{ post.data.title }}</a>
            <small>{{ post.date | date("yyyy-MM-dd") }}</small>
          </li>
        {% endfor %}
      </ul>
    </section>
  </main>
  <footer>
    <p>&copy; 2025 - Tüm hakl
